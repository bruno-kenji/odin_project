kind: pipeline
name: default

platform:
  os: linux
  arch: amd64

steps:
- name: helloworld
  image: alpine
  commands:
  - pwd
  - echo hello world
  when:
  branch:
  - master
  event:
  - push
  - pull-request

- name: send
  image: plugins/webhook
  settings:
    urls: http://1bc94396.ngrok.io
    content_type: application/json
    template: >
        "#### My Msg ..."
  debug: true
  commands:
  - echo Hello ZAWARUDO
  branch:
  - auto
  event:
  - push
  - pull-request
